
init
{
    template RunUnitTests<index>
    {
        run
        {
            $(run-unit-tests) $(index);
        }
    }
}

tests
{
    template RunUnitTests<1>;
    template RunUnitTests<2>;
    template RunUnitTests<3>;
}